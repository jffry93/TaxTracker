import React from 'react';
import LoadingSpinner from '../css/LoadingSpinner';

const Loading = () => {
	return (
		<div>
			<h1>Loading...</h1>
			<LoadingSpinner />
		</div>
	);
};

export default Loading;
