import React from 'react';
import LoadingSpinner from '../styles/LoadingSpinner';

const Loading = () => {
	return (
		<div>
			<h1>Loading...</h1>
			<LoadingSpinner />
		</div>
	);
};

export default Loading;
